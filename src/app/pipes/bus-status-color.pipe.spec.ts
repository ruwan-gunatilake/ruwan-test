import { BusStatusColorPipe } from './bus-status-color.pipe';

describe('BusStatusColorPipe', () => {
  it('create an instance', () => {
    const pipe = new BusStatusColorPipe();
    expect(pipe).toBeTruthy();
  });
});
